<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_auth.xml — Container activity for the authentication flow.
    Hosts the Navigation Component fragment back-stack for:
    Guest → Login / Register → OTP → [Navigate to MainActivity]

    This is intentionally minimal — all content is rendered inside fragments.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary">

    <!-- NavHostFragment renders each auth screen fragment here -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/auth_nav_host"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:navGraph="@navigation/auth_nav_graph"
        app:defaultNavHost="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
